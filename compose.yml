services:
  docs:
    build:
      context: ..
      dockerfile: docsa/Dockerfile
    ports:
      - "127.0.0.1:3001:80"
    volumes:
      - ../docsa/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
