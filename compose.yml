services:
  docs:
    build:
      context: ..
      dockerfile: doc-builder/Dockerfile
    ports:
      - "127.0.0.1:3001:80"
    volumes:
      - ../docs:/docs
      - ../doc-builder/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
